# 문제
리코쳇 로봇이라는 보드게임이 있습니다.
이 보드게임은 격자모양 게임판 위에서 말을 움직이는 게임으로, 시작 위치에서 출발한 뒤 목표 위치에 정확하게 멈추기 위해 최소 몇 번의 이동이 필요한지 말하는 게임입니다.
이 게임에서 말의 이동은 현재 위치에서 상, 하, 좌, 우 중 한 방향으로 게임판 위의 장애물이나 게임판 가장자리까지 부딪힐 때까지 미끄러져 움직이는 것을 한 번의 이동으로 정의합니다.
다음은 보드게임판을 나타낸 예시입니다. ("."은 빈 공간을, "R"은 로봇의 처음 위치를, "D"는 장애물의 위치를, "G"는 목표지점을 나타냅니다.)


# 접근방법
1. 격자내에서 이동하는 것 .격자문제의 기본틀을 생각하자
  -상하좌우 정의하기
- 이동가능한 거리가 무엇인지 생각하기 +탐색 전에 인덱스 범위 먼저 정의해서 터지지 않도록 (이동시 주어진 격자 나가지 않게)
2. 격자 문제+최소거리이니 이것도 전형적인 BFS문제. 이전에 풀었던 week4의 1,2문제풀이와 동일한 방식임.
3. 만약에 답이 나오지 않는다면 return -1 꼭 해주기



# sol1
from collections import deque

def solution(board):
    
    #변수들 정의하기 
    row=len(board)
    col=len(board[0])
    
    dr=[0,0,1,-1]
    dc=[1,-1,0,0]
    
    visited=[[False]*col for _ in range(row)]
    sr,sc=0

    #시작점 찾기 
    for r in range(row):
        for c in range(col):
            if board[row][col]==R:
                sr=row
                sc=col
    queue=deque([(sr,sc,0)])
    
    #탐색 시작 
    
    while queue:
        r,c,distance=queue.popleft()
        visited[r][c]=True
        
        if boards[r][c] == 'G':
            return distance
        
        
        for i in range(4):
            nr=r+dr[i]
            nc=c+dc[i]
            
            if 0<=nr<row and 0<=nc<col and board[r][c]!='D':
                if visited[nr][nc]==False:
                    queue.append((nr,nc,distance+1))
                
    
    return -1



#틀린점 
리코쳇 로봇은 '미끄러짐'이란 특성이 있는 것을 전혀 고려하지않았음.. 문제를 꼼꼼하게읽자 

# sol2 정답 

#시간복잡도

#공간복잡도 

#배운점 



